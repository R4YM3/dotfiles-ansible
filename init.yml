- hosts: localhost
  vars:
    CONFIG_DIR: '{{ ansible_env.HOME }}/.config'
    ALIASES_DIR: '{{ CONFIG_DIR }}/zsh/aliases'
  roles:
    - role: macos
    - role: cli
    # - role: pip
    - role: homebrew_cask

    - role: apt
    - role: snap

    - role: ack
    - role: curl
    - role: git
    - role: tmux
    - role: tmuxinator
    - role: vim
    - role: wget
