name: dotfiles-ansible
root: ~/Development/hetwebbureau/dotfiles-ansible

windows:
  - Editor:
      layout: main-vertical
      panes:
        - left:
          - vim
        - runserver:
          - git status
        - git:
          - git status
